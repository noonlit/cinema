<?php
namespace Entity;

class MovieValidator {
    
    // can't the movie entity have something like a method isValid() that returns true if all the properties are set? or ... ? not sure what goes in here/there
    
}

